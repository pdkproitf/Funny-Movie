<div class="shares">
  <%= render partial: 'share', locals: {share: @share} %>

  <p>
    <%= link_to 'Edit |', edit_share_path(@share) if editable?(@share) %>
    <%= link_to 'Back', shares_path %>
  </p>
</div>
